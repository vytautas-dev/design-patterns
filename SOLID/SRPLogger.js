export default function logMessage(message) {
	console.log(message);
	// logMessage also has one reason to change, it's main responsibility is to notify the user about calories, for now we're logging the message, but we could also notify them via email
}
